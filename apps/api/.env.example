# Server Configuration
PORT=3001
NODE_ENV=development
WEB_ORIGIN=http://localhost:3000
API_ORIGIN=http://localhost:3001

# Debug Configuration (optional)
# Set to 'true' to enable verbose Prisma query logging
DEBUG_QUERIES=false

# Database Configuration with Connection Pooling
# PostgreSQL connection URL with recommended pooling parameters:
# - connection_limit: Max connections in pool (default: num_physical_cpus * 2 + 1)
#   * Development: 5-10 connections
#   * Production: 10-20 connections (adjust based on load)
# - pool_timeout: Max wait time for connection from pool in seconds (default: 10)
# - connect_timeout: Max time to establish new connection in seconds (default: 5)
#
# Example for production:
# DATABASE_URL=postgresql://user:password@host:5432/dbname?connection_limit=20&pool_timeout=10&connect_timeout=5
#
# Example for development:
DATABASE_URL=postgresql://user:password@host:5432/dbname?connection_limit=10&pool_timeout=10&connect_timeout=5

# Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Redis Cache (optional - falls back to in-memory cache if not set)
REDIS_URL=redis://localhost:6379

# Movie API Provider Configuration
# Supported providers: 'tmdb' (default) or 'omdb'
# Switch providers without code changes - see apps/api/src/modules/movies/README.md
MOVIE_API_PROVIDER=tmdb

# TMDB API (The Movie Database)
# Free for non-commercial use, $149/month for commercial
# Get key: https://www.themoviedb.org/settings/api
TMDB_API_KEY=your-tmdb-api-key
TMDB_API_URL=https://api.themoviedb.org/3

# OMDb API (Open Movie Database) - Phase 2
# Cost: $1-10/month for commercial use
# Get key: https://www.omdbapi.com/apikey.aspx
# OMDB_API_KEY=your-omdb-api-key

# Sentry Error Monitoring (optional - leave empty to disable)
# Get your DSN from https://sentry.io/settings/projects/
# Format: https://<key>@<organization>.ingest.sentry.io/<project>
SENTRY_DSN=

# Stripe Payment Processing (Phase 2)
# Get keys from https://dashboard.stripe.com/test/apikeys
# IMPORTANT: Use test keys (sk_test_...) during development
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Stripe Product Price IDs (Phase 2)
# Create products/prices in Stripe Dashboard and paste IDs here
# Format: price_xxxxxxxxxxxxx
STRIPE_PRICE_STARTER=price_starter_monthly_id
STRIPE_PRICE_PRO=price_pro_monthly_id
STRIPE_PRICE_TEAM=price_team_monthly_id
