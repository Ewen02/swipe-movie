// SwipeMovie - Shared Prisma Schema
// This is the single source of truth for the database schema

datasource db {
  provider  = "postgresql"
  // Runtime queries go through the pooled (PgBouncer) endpoint on Vercel.
  url       = env("DATABASE_URL")
  // Migrations / introspection use the direct, non-pooled endpoint.
  // PgBouncer in transaction mode cannot hold the advisory lock that
  // `migrate deploy` needs (causes P1002). Falls back to DATABASE_URL locally.
  directUrl = env("DIRECT_URL")
}

generator client {
  provider = "prisma-client-js"
}

// ==========================================
// Enums
// ==========================================

enum RoomType {
  MOVIE
  TV
}

// ==========================================
// User & Authentication (Better Auth)
// ==========================================

model User {
  id               String             @id @default(cuid())
  email            String             @unique
  name             String?
  emailVerified    Boolean            @default(false)
  image            String?
  roles            String[]           @default([])
  isGuest          Boolean            @default(false)
  stripeCustomerId String?            @unique

  // Guest conversion tracking. Set when a guest signs up and migrateGuestToUser
  // promotes them to a real account. Lets the admin separate organic signups
  // from trial-driven signups and compute a true guest→user conversion rate.
  convertedFromGuestAt        DateTime?
  guestSwipesAtConversion     Int?
  guestCreatedAt              DateTime?

  // User preferences (onboarding)
  watchProviders      Int[]     @default([])
  watchRegion         String    @default("FR")
  favoriteGenreIds    Int[]     @default([])
  onboardingStep      Int       @default(0) // 0=not started, 1=providers, 2=genres, 3=swipes, 4=completed
  onboardingCompleted Boolean   @default(false)

  members           RoomMember[]
  swipes            Swipe[]
  subscriptions     Subscription[]
  sessions          Session[]
  accounts          Account[]
  mediaLibrary      UserMediaLibrary[]
  pushSubscriptions PushSubscription[]
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt

  @@index([isGuest])
  @@index([convertedFromGuestAt])
}

// A Web Push subscription (one per browser/device). Created when a user opts in
// to push notifications; the cron/notification senders fan out over all of a
// user's subscriptions. Dead endpoints (410/404 from the push service) are
// pruned on send.
model PushSubscription {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  // The push endpoint URL is globally unique per browser subscription.
  endpoint  String   @unique
  // Keys needed to encrypt the payload (from the browser PushSubscription).
  p256dh    String
  auth      String

  createdAt DateTime @default(now())

  @@index([userId])
}

model Session {
  id        String   @id @default(cuid())
  userId    String
  token     String   @unique
  expiresAt DateTime
  ipAddress String?
  userAgent String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([token])
}

model Account {
  id                    String    @id @default(cuid())
  userId                String
  accountId             String
  providerId            String
  accessToken           String?
  refreshToken          String?
  accessTokenExpiresAt  DateTime?
  refreshTokenExpiresAt DateTime?
  scope                 String?
  idToken               String?
  password              String?
  lastSyncAt            DateTime?
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt
  user                  User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, providerId], name: "userId_providerId")
  @@index([userId])
  @@index([providerId, accountId])
}

model Verification {
  id         String   @id @default(cuid())
  identifier String
  value      String
  expiresAt  DateTime
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@index([identifier])
}

// ==========================================
// Rooms & Swipes
// ==========================================

model Room {
  id             String   @id @default(cuid())
  code           String   @unique
  name           String   @default("Untitled Room")
  capacity       Int      @default(10)
  type           RoomType @default(MOVIE)
  genreId        Int      @default(0)
  selectedTmdbId Int?

  // Advanced filters
  minRating        Float?
  releaseYearMin   Int?
  releaseYearMax   Int?
  runtimeMin       Int?
  runtimeMax       Int?
  watchProviders   Int[]   @default([])
  watchRegion      String? @default("FR")
  originalLanguage String?

  // Recurring room settings
  isRecurring       Boolean   @default(false)
  lastResetAt       DateTime?
  recurringInterval String?   @default("monthly") // "weekly", "monthly", or null

  members   RoomMember[]
  matches   Match[]
  swipes    Swipe[]
  createdBy String
  createdAt DateTime     @default(now())
  updatedAt DateTime     @default(now()) @updatedAt
  deletedAt DateTime?

  // Set once the "your room is about to expire" reminder has been emailed, so
  // the hourly reminder cron never notifies the same room twice.
  expiryReminderSentAt DateTime?

  @@index([type, genreId])
  @@index([deletedAt])
  @@index([createdBy])
}

model RoomMember {
  id     String @id @default(cuid())
  roomId String
  userId String
  room   Room   @relation(fields: [roomId], references: [id], onDelete: Cascade)
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([roomId, userId])
  @@index([userId])
}

model Swipe {
  id        String   @id @default(cuid())
  roomId    String
  userId    String
  movieId   String
  value     Boolean
  createdAt DateTime @default(now())

  room Room @relation(fields: [roomId], references: [id], onDelete: Cascade)
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([roomId, userId, movieId], name: "userId_roomId_movieId")
  @@index([roomId])
  @@index([userId])
  @@index([roomId, movieId])
  @@index([roomId, createdAt]) // For analytics daily activity queries
  @@index([userId, createdAt]) // For user stats today queries
}

model Match {
  id        String   @id @default(cuid())
  roomId    String
  movieId   String
  createdAt DateTime @default(now())
  room      Room     @relation(fields: [roomId], references: [id], onDelete: Cascade)

  @@unique([roomId, movieId])
  @@index([roomId])
  @@index([createdAt])
  @@index([movieId])
}

// ==========================================
// External Services (Trakt, AniList)
// ==========================================

model UserMediaLibrary {
  id         String    @id @default(cuid())
  userId     String
  tmdbId     String
  mediaType  String // TS enum MediaType: "movie" | "tv"
  status     String // TS enum MediaStatus: "watched" | "watchlist" | "rated" | "liked" | "disliked"
  rating     Float?
  source     String // TS enum MediaSource: "trakt" | "anilist" | "manual" | "onboarding" | "discover"
  externalId String?
  watchedAt  DateTime?
  importedAt DateTime  @default(now())
  updatedAt  DateTime  @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, tmdbId, mediaType])
  @@index([userId, status])
  @@index([userId, source])
  @@index([userId, mediaType])
}

// ==========================================
// Subscriptions (Better Auth Stripe)
// ==========================================

model Subscription {
  id                   String    @id @default(cuid())
  plan                 String
  referenceId          String
  stripeCustomerId     String?
  stripeSubscriptionId String?
  status               String    @default("incomplete")
  periodStart          DateTime?
  periodEnd            DateTime?
  cancelAtPeriodEnd    Boolean   @default(false)
  seats                Int?
  trialStart           DateTime?
  trialEnd             DateTime?
  createdAt            DateTime  @default(now())
  updatedAt            DateTime  @updatedAt
  deletedAt            DateTime?

  user User @relation(fields: [referenceId], references: [id], onDelete: Cascade)

  @@index([referenceId])
  @@index([plan])
  @@index([status])
  @@index([stripeCustomerId])
  @@index([stripeSubscriptionId])
}
